<% if team.present? %>
  <span class="<%= team.hot? ? 'hot_team' : '' %>">
    <%= team.name %>
    <%= "(#{team.ap_rank})" if team.ap_rank? %>
    <%= "[#{team.rank}]" if team.ranked? %>
  </span>
<% else %>
    [TBD]
<% end %>